services:
  backend:
    build: ./backend # ← le dice a Docker que use el Dockerfile dentro de ./backend
    container_name: crud_backend
    ports:
      - "8080:8080"
    env_file:
      - .env
    depends_on:
      - mongo
    networks:
      - app-network

  mongo:
    image: mongo:6
    container_name: mongo_db
    restart: always
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db
    networks:
      - app-network

  frontend:
    build: ./frontend
    container_name: frontend
    volumes:
      - frontend_build:/app/build
    command: sh -c "npm run build && sleep 1"

  nginx:
    image: nginx:alpine
    container_name: nginx
    ports:
      - "80:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - frontend_build:/usr/share/nginx/html:ro
    depends_on:
      - backend

volumes:
  mongo-data:
    driver: local
  frontend_build:
    driver: local

networks:
  app-network:
    driver: bridge
